import Admin from "./pages/Admin";

// The admin app is a single-page application — every route renders the
// Admin component, which handles its own login/auth state internally.
export default function App() {
  return <Admin />;
}
